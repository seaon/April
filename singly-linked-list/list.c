#include <stdio.h>
#include "list.h"
// #include <stdlib.h>

int main(int argc, char const *argv[])
{
    Node node;
    node.value = 1;

    printf("%d\n", node.value);
    return 0;
}

int len(Node node)
{
    int length = 0;

    return length;
}